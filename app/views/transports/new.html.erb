<% if @transport.errors.any? %>
	<%= t('transports.create.messages.not_saved') %>
<% end %>

<h1><%= t('transports.new.title') %></h1>

<%= form_for @transport do |f| %>
	<%= render :partial => 'form', :locals => {:f => f} %>
<% end %>